{
    "options": {
        // 这里指定tasks 的运行目录，默认是${workspaceRoot}，也就是.vscode/..
        "cwd": "${workspaceRoot}/build"
    },

    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ 生成活动文件",
            "command": "./buil.sh",
            "args": [],
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "调试器生成的任务。"
        }

    ],
    "version": "2.0.0"
}